import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    maxWidth: '100%',
    width: 'auto',
    flexDirection: 'row',
    justifyContent: 'center',
    marginTop: 30,
    flexWrap: 'wrap',
  },
  pageContainer: {
    flexDirection: 'row',
  },
});
